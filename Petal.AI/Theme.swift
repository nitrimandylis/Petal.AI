import SwiftUI

struct Theme {
    static let primaryPink: Color = Color.pink.opacity(0.6)
    static let secondaryPink: Color = Color.pink.opacity(0.15)
    static let secondaryColor: Color = .white
    static let accentColor: Color = .cyan
    static let streakCardBackground: Color = Color.pink.opacity(0.2)
    static let chatCardBackground: Color = Color.pink.opacity(0.2)
    static let overlayBackground: Color = Color.black.opacity(0.2)
    static let backgroundColor: Color = Color.pink.opacity(0.3)
    
    static let cornerRadius: CGFloat = 25
    static let padding: CGFloat = 16
    static let cardSpacing: CGFloat = 12
}
